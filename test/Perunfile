function echo_foo() {
  echo "foo"
}

function echo_bar_and_args() {
  echo "bar" "$@"
}

function echo_empty_or_first_arg() {
    local a
    a=$1
    if [[ $a == "" ]]; then
        echo empty
    else
        echo "$a"
    fi
}
